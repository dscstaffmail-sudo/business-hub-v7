# ローカルで実行
mkdir business-hub-v7
cd business-hub-v7

# package.json 作成
cat > package.json << 'EOF'
{
  "name": "business-hub-v7",
  "version": "7.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "lucide-react": "^latest"
  }
}
EOF

# business-hub-v7-enterprise.jsx をコピー
cp /mnt/user-data/outputs/business-hub-v7-enterprise.jsx src/App.jsx

# GitHub へプッシュ
git init
git add .
git commit -m "Business Hub v7 Enterprise - Complete Version"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/business-hub-v7.git
git push -u origin main
